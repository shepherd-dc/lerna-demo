{
  "name": "root",
  "private": true,
  "scripts": {
    "serve": "lerna exec --scope use-component-demo -- npm run serve",
    "build": "lerna exec --scope ui-component -- npm run build",
    "lint": "vue-cli-service lint",
    "build:demo": "lerna exec --scope use-component-demo -- npm run build",
    "build:util": "lerna exec --scope ui-common -- npm run build",
    "dev": "lerna exec --scope ui-component -- npm run dev",
    "dev:util": "lerna exec --scope ui-common -- npm run dev"
  },
  "devDependencies": {
    "@vue/cli-plugin-eslint": "^4.5.13",
    "@vue/eslint-config-standard": "^5.1.2",
    "eslint": "^6.7.2",
    "eslint-plugin-import": "^2.20.2",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.0",
    "eslint-plugin-vue": "^6.2.2",
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
